---
- name: Provisioning configure app
  hosts: app 
  become: true
  tasks:
    - name: Install ruby and rubygems and required packages
      apt:
        update_cache: yes
        name: "{{ packages }}"
      vars:
         packages:
         - ruby-full
         - ruby-bundler
         - build-essential

